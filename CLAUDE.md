# CLAUDE.md — Ovora Cargo Mobile

Контекст проекта для Claude Code. Читай этот файл в начале каждой сессии.

---

## Стек и архитектура

| Слой | Технология |
|---|---|
| Frontend | React 18 + TypeScript + Vite 6 + Tailwind CSS v4 |
| Роутинг | React Router v7 |
| Анимации | Framer Motion (`motion/react`) |
| Backend | Supabase Edge Functions (Deno/Hono) |
| База данных | Postgres-таблицы для поездок, заявок, грузов и откликов (MIGR-1, `bookingStore.tsx`); остальное — KV Store |
| Хранилище файлов | Supabase Storage |
| Email | SMTP через edge function |
| Push | Web Push API (VAPID) |
| OCR | OCR.space API |
| Карты | Yandex Maps API v3 |

**Это PWA** (Progressive Web App), не нативное мобильное приложение.

---

## Git и деплой

**Репозиторий:** `https://github.com/xsxsss/ovora-cargo.git` (remote `origin`, ветка `main`).

Старый аккаунт `magamed99` заблокирован GitHub — вместе с ним оказался
заблокирован и привязанный к нему Vercel-проект `dly-a-prid` (`live: false`,
все деплои `BLOCKED`, снять паузу через API нельзя — 403). Поэтому проект
переехал на новый аккаунт.

**Живой сайт:** https://ovora-cargo.saburov.workers.dev (Cloudflare Workers, из корня `/`).
Cloudflare собирает сам на push в `main` (`npm run build` → `npx wrangler deploy`, настройки в `wrangler.jsonc`,
заголовки безопасности — `public/_headers`). Переменные сборки (`NODE_VERSION`, `VITE_YANDEX_METRIKA_ID`,
`VITE_SENTRY_DSN`) заданы в кабинете Cloudflare → Settings → Builds, не в репозитории.
Ветки получают тестовые адреса `<версия>-ovora-cargo.saburov.workers.dev` — они разрешены в CORS.

Старый адрес https://xsxsss.github.io/ovora-cargo/ (GitHub Pages) пока работает параллельно: GitHub запрещает
Pages для коммерческих сервисов, выключить после переезда пользователей. Новый адрес сайта — добавлять в
`ALLOWED_ORIGINS` (`index.ts`), в `public/_headers` и в настройки счётчика Метрики.

**Токены Supabase в GitHub** — у каждого проекта свой (токен создаётся с доступом к одному проекту):
`SUPABASE_ACCESS_TOKEN1` — боевой `mkbcjxnoeevtkzaqcpsh`, `SUPABASE_ACCESS_TOKEN` — тестовый `xrtqquuwlnnihphszyns`.
Выпуск сервера из `main` без `SUPABASE_ACCESS_TOKEN1` падает с 403 — сайт при этом выходит, а сервер нет.

**Тестовая площадка (staging):** `git push origin HEAD:staging` → сервер уходит в тестовый проект Supabase
`xrtqquuwlnnihphszyns`, сайт — на `staging-ovora-cargo.saburov.workers.dev` с плашкой «ТЕСТОВАЯ ВЕРСИЯ».
База выбирается по адресу страницы (`utils/supabase/info.ts`): версии веток и localhost — тестовая, остальное —
боевая. Порядок выпуска: staging → проверить → main. Секреты тестового проекта задаёт владелец отдельно,
значения боевых туда не копировать.

**Ссылка отписки от писем** подписана (`unsubscribeLink.tsx`, секрет `USER_JWT_SECRET`) и ведёт на страницу
сайта `/unsubscribe`. HTML из edge-функции не показывать: Supabase отдаёт его как `text/plain`.

**Деплой:**
```bash
git push origin HEAD:main    # пуш → Cloudflare и GitHub Pages собирают сайт, бэкенд уходит в Supabase
```

**Base-путь определяется автоматически** — не зашивай его константой:
- GitHub Pages отдаёт из подпапки, workflow подставляет `VITE_BASE=/<имя репо>/`
- Vercel отдаёт из корня — база остаётся `/`
- Роутер берёт базу из `import.meta.env.BASE_URL`
- `service-worker.js` и `404.html` вычисляют её из своего адреса (Vite их не обрабатывает)

**Локальная проверка собранной версии:**
```bash
npm run build && npm run preview   # http://localhost:4173
```

---

## Ключевые файлы

| Файл | Назначение |
|---|---|
| `src/app/components/Welcome.tsx` | Главная страница — десктоп/мобайл |
| `src/styles/index.css` | CSS Grid Welcome-страницы, анимации |
| `src/app/i18n/translations.ts` | Переводы ru/tj/en (167 ключей) |
| `src/vite-env.d.ts` | TypeScript global declarations |
| `supabase/functions/make-server-4e36197a/index.ts` | Весь backend API (~7300 строк) |
| `supabase/functions/make-server-4e36197a/rateLimit.tsx` | Token bucket rate limiter |
| `supabase/functions/make-server-4e36197a/kv_store.tsx` | KV абстракция |
| `supabase/functions/make-server-4e36197a/email.tsx` | Email шаблоны |
| `netlify.toml` | Netlify конфиг + CSP/HSTS headers |
| `vercel.json` | Vercel конфиг + CSP/HSTS headers |
| `index.html` | CSP meta-тег (fallback для GitHub Pages) |
| `public/manifest.json` | PWA манифест |
| `.github/workflows/deploy-pages.yml` | CI: typecheck → build → deploy |

---

## Welcome-страница — раскладка (десктоп 700px+)

**CSS Grid 3-колонки × 3-строки** (`src/styles/index.css`):
```
[lang selector]  [пусто — hero просвечивает]  [AVIA карточка ]
[Платформа Ovora]                             [CARGO карточка]
[features bar ————————————— partners card         ————————————]
```

Grid области:
- `.ovora-area-lang`   → col 1 / row 1
- `.ovora-area-brand`  → col 1 / row 2 (скрыт на мобиле)
- `.ovora-area-cards`  → col 3 / rows 1-2
- `.ovora-area-bottom` → col 1/-1 / row 3

**Hero-оверлей:** три направленных градиента (left/right/bottom) скрывают грузовик
`OVORA-CARGO` из фоновой картинки чтобы он не просвечивал через левую колонку.

На мобиле: flex column, порядок → cards → lang → bottom.

---

## Backend API — паттерны безопасности

Все эндпоинты: `POST /make-server-4e36197a/<route>`

### Admin middleware + RBAC
```ts
// Legacy plaintext (backward-compat) — всегда роль super-admin:
X-Admin-Code: <plaintext>

// Role-scoped JWT (подписан ADMIN_JWT_SECRET, 8 часов) — отдельный заголовок,
// т.к. Authorization зарезервирован под Supabase anon key:
X-Admin-Token: <jwt>          // payload: { role: 'super-admin' | 'cargo-admin' | 'avia-admin' }
```
- `super-admin` проходит любую `requireRole(...)` проверку (полный доступ).
- `cargo-admin` — доступ только к `/admin/*` (CARGO + общие разделы).
- `avia-admin` — доступ только к `/avia/admin/*`.
- `cargo-admin`/`avia-admin` работают **только** через JWT — `ADMIN_JWT_SECRET` обязателен, иначе `/admin/auth` отказывает в выдаче токена для этих ролей.
- Фронтенд: роль и токен хранятся в `sessionStorage` (`ovora_admin_role`, `ovora_admin_jwt`/`ovora_admin_token`), `AdminLayout.tsx` фильтрует `navGroups` по роли.

### Правила авторизации (применены)
- **Личность — только из `X-User-Token`.** `callerEmail`, `userEmail`, `driverEmail`, `senderId`
  в теле, адресе или форме — просто текст. Читать через `actingAs(c, claimed)` /
  `isActingAs(c, email)` / `getCallerEmail(c, body)` в `index.ts`. Без `USER_JWT_SECRET` они
  возвращают заявленное (legacy), в проде секрет настроен.
- Фронт шлёт токен через `withUserToken(...)` / `userTokenHeader()` (`src/app/api/userToken.ts`),
  а любой POST/PUT/PATCH/DELETE — ещё и CSRF-заголовок, иначе 403.
- `PUT /auth/user` — нельзя менять: `role`, `status`, `codeHash`, `blocked`, `isVerified`, `passportNumber`, `passportData`
- Чат: читать, удалять и менять предложение может только участник; принять — только водитель поездки
- `PUT /offers`, `PUT /cargo-offers` — только статус и только по ролям из `capacity.tsx`
- `PUT /cargos/:id` — только владелец и только поля из `CARGO_OWNER_EDITABLE`, без `status`

### Rate limits (из `rateLimit.tsx`)
| Эндпоинт | Лимит |
|---|---|
| `/auth/login-email` | 15 req / 5 мин |
| `/auth/login-phone` | 15 req / 5 мин |
| `/auth/register` | 3 req / час |
| `/admin/auth` | 15 req / 5 мин |

### CORS
Разрешены только: `ovora-cargo.ru` (+ поддомены), `xsxsss.github.io`, `localhost:5173/4173`

**Важно:** при переезде сайта на новый домен его нужно добавить в `ALLOWED_ORIGINS`
(`index.ts`), иначе браузер получит `Failed to fetch` — бэкенд будет жив, но
отклонит запросы с неизвестного origin.

---

## Переменные окружения (Supabase Secrets)

| Переменная | Назначение |
|---|---|
| `SUPABASE_URL` | URL проекта Supabase |
| `SUPABASE_SERVICE_ROLE_KEY` | Service role ключ |
| `ADMIN_ACCESS_CODE` | Пароль для `/admin/auth` → роль `super-admin` |
| `ADMIN_ACCESS_CODE_CARGO` | Пароль для роли `cargo-admin` (опционально, RBAC) |
| `ADMIN_ACCESS_CODE_AVIA` | Пароль для роли `avia-admin` (опционально, RBAC) |
| `ADMIN_JWT_SECRET` | Секрет для подписи JWT токенов (минимум 32 символа) |
| `AVIA_JWT_SECRET` | Секрет для подписи AVIA session-токенов (`X-Avia-Token`, минимум 32 символа) |
| `SUPABASE_ANON_KEY` | Anon key — нужен для вызова GoTrue (`/auth/v1/otp`, `/auth/v1/verify`) из бэкенда |
| `USER_JWT_SECRET` | Секрет для подписи CARGO session-токенов (`X-User-Token`, минимум 32 символа) |
| `YANDEX_GEOCODER_API_KEY` | Yandex Geocoder API |
| `OCR_SPACE_API_KEY` | OCR.space для распознавания документов |

**Статус секретов (проверено по логам продакшена 2026-09-13):**
- `ADMIN_JWT_SECRET` — **настроен**. В логах есть `Access granted (role=cargo-admin)` и `(role=avia-admin)`, а эти роли выдаются только через JWT.
- `AVIA_JWT_SECRET` — **настроен**. Входы в AVIA идут без предупреждения `AVIA_JWT_SECRET not configured`.
- `USER_JWT_SECRET` — **настроен**. На каждом входе в CARGO без секрета писалось бы `USER_JWT_SECRET not configured` — таких записей нет. Поэтому `getCallerEmail()` игнорирует `callerEmail` из тела запроса (legacy-фолбэк выключен).

**Капча Supabase Auth должна быть выключена** (Authentication → Attack Protection). Сервер вызывает
GoTrue `/auth/v1/otp` без `captcha_token`: при включённой капче код на почту не уходит — регистрация и вход
по коду ломаются молча (так было до 2026-09-15). Включать только вместе с капчей на фронте.

**Оповещения об ошибках — Telegram** (`alerts.tsx`, секрет `TELEGRAM_BOT_TOKEN`). Sentry из России и
Таджикистана недоступен (403). Любой ответ 5xx копится в KV `ovora:alerts:pending`, сводка владельцу — не чаще раза
в 15 минут. Чат владельца запоминается один раз в `ovora:alerts:telegram_chat_id`; сменить — удалить этот ключ и
написать боту заново. Ошибку, которую сервер «проглотил» и ответил 200, бот не увидит — отвечай 5xx.

**Резервные копии:** `pg_cron` задача `weekly-kv-backup` — по понедельникам 03:00 UTC копирует KV в схему
`backup` (закрыта от API), хранятся 4 последние. Восстановление — вручную из `backup.kv_store_4e36197a_ГГГГММДД`.

**Не делай выводов о секретах по коду или по этому файлу — проверяй логи.** Без секрета
модули пишут предупреждение на каждом входе, так что его отсутствие в `function_logs` —
надёжный признак, что секрет задан:
```sql
select event_message from logs where source = 'function_logs'
  and event_message like '%JWT_SECRET not configured%'
```
Значения секретов не спрашивай и не проси вставить в чат — их добавляет владелец сам.

---

## Переводы (i18n)

Файл: `src/app/i18n/translations.ts`
Языки: `ru` | `tj` | `en` — **167 ключей** (проверено 2026-09-13), все три языка полностью покрыты.

Проверка синхронности переводов (на Windows вместо `python3` — `python`):
```bash
python3 -c "
import re
with open('src/app/i18n/translations.ts') as f: c = f.read()
def keys(lang):
    m = re.search(rf'{lang}:\s*\{{(.*?)^\s*\}},', c, re.DOTALL|re.MULTILINE)
    return set(re.findall(r'^\s+(\w+)\s*:', m.group(1), re.MULTILINE)) if m else set()
ru,tj,en = keys('ru'),keys('tj'),keys('en')
print(f'RU:{len(ru)} TJ:{len(tj)} EN:{len(en)}')
print('Missing TJ:', sorted(ru-tj))
print('Missing EN:', sorted(ru-en))
"
```

---

## TypeScript

```bash
npm run typecheck   # tsc --noEmit — 0 ошибок
npm run build       # сборка через Vite/esbuild — не делает type-check
```

- `tsconfig.json` создан с `strict: true`
- `typescript`, `@types/react`, `@types/react-dom` установлены в devDependencies
- CI запускает `typecheck`, `test` и `lint` перед build — **все три блокируют деплой** (снято «пропускать ошибки» 2026-09-13, когда ошибок стало 0)
- Известный реальный баг исправлен: `TripDetail` — отсутствовал `useNavigate()`

---

## Безопасность — сделано в этой сессии

### Edge Function (все в `index.ts`)
| ID | Уязвимость | Исправление |
|---|---|---|
| C-2 | Privilege escalation `PUT /auth/user` | Whitelist полей, блокировка `role`/`status`/`codeHash` |
| H-1 | IDOR chat messages | Проверка `participants` на всех чат-эндпоинтах |
| H-2 | PII leak в login | Удаляем `codeHash`, `passportNumber`, `passportData` из ответа |
| H-3 | IDOR `PUT /cargos/:id` | Проверка `callerEmail === senderEmail` |
| H-4 | Yandex key публичный | `requireAdmin` на `/config/yandex-key` |
| H-5 | Plaintext admin token | JWT (HS256, 8ч) через `npm:jose` |
| H-7 | `/test-ocr` без auth | `requireAdmin` |
| M-4 | `callerEmail` опциональный | Обязателен в `/offers` и `/reviews` |
| M-5 | `/chats/cleanup-demo` | `requireAdmin` |
| M-6 | `/ocr/scan-document` cost hijack | Проверка `callerEmail` + существование юзера |
| CORS | `origin: "*"` | Allowlist доменов |
| Rate | Нет лимитов на auth | `RL.LOGIN` + `RL.REGISTER` |

### Frontend
| Что | Файл |
|---|---|
| CSP + HSTS + Permissions-Policy | `netlify.toml`, `vercel.json`, `index.html` |
| SPA redirect вынесен из inline | `public/spa-redirect.js` |
| Дублирующий `@import fonts.css` | Удалён из `src/styles/index.css` |
| PWA meta tags | `index.html`, `manifest.json` |

---

## Журнал админки — кто что делал

Два журнала, по одному на площадку: `cargoAudit.tsx` (`ovora:cargo-audit:*`) и
`aviaAudit.tsx` (`ovora:avia-audit:*`). В главной админке они собраны в папку
«Аудит»; сотрудник площадки видит в ней только свой журнал.

- Актор админских записей — `admin:<роль>`, роль берётся из проверенного токена
  (`adminActor(c)` в `index.ts` и `aviaRoutes.tsx`). Просто `admin` писать нельзя —
  по такой записи не отличить директора от сотрудника.
- `adminActor(c)` попутно ставит `c.set('auditLogged', true)`. Вызывать его
  **только** внутри `AuditLog.record(...)` — иначе сквозной журнал решит, что
  запрос уже записан, и пропустит его.
- Сквозной журнал (`auditFallback` в `index.ts`) пишет `admin.request` для любого
  POST/PUT/PATCH/DELETE к `/admin/*` и `/avia/admin/*`, который обработчик не
  залогировал подробно. Новый раздел админки нельзя забыть залогировать.
- Вход в админку — `admin.login` (в самом `/admin/auth`), с IP и user-agent,
  включая неудачные попытки подбора кода.

## Данные о человеке и устройстве

- `authIdentity.tsx` — дописывает в карточку Supabase Auth (Authentication → Users)
  Display name и Phone через Admin API. Без этого в дашборде они пустые: в GoTrue
  уходит только email. Вызывается из `/auth/register` и `PUT /auth/user`.
  id пользователя в Auth запоминается в KV (`ovora:auth_uid:<email>`) при проверке
  кода из письма — чтобы не искать его по всей таблице.
- `deviceInfo.tsx` — разбирает User-Agent (браузер, ОС, модель телефона) и хранит
  последние 5 входов в `ovora:device:<platform>:<id>`. Пишется при входе CARGO
  (`verify-perm-code`) и AVIA (`/avia/login`), читается в админке
  (`GET /admin/users/:email/devices`, `GET /avia/admin/users/:phone/devices`).
  Это персональные данные — наружу не отдаются.

## Кеш привязан к аккаунту

`src/app/api/sessionScope.ts` — `claimCacheOwner('cargo:<email>' | 'avia:<phone>')`
вызывается в момент входа и стирает весь пользовательский localStorage, если
устройство было закреплено за другим аккаунтом.

Так закрыт баг «в одном приложении зашли в два аккаунта — второй видел переписку
первого»: чаты, сообщения и поездки лежат под общими ключами, а старая очистка
сверялась с `sessionStorage`, который умирает вместе с вкладкой.

Добавляя новый ключ в localStorage: всё, что начинается на `ovora`, считается
пользовательским и стирается при смене аккаунта. Настройки устройства (язык,
звук, вибрация) перечислены в `DEVICE_KEYS` — их дополняй явно.

## Команды

```bash
npm run dev          # dev сервер
npm run build        # production build → dist/
npm run typecheck    # TypeScript проверка

npm run lint         # ESLint — 0 ошибок (предупреждения не блокируют)
npm run test         # Vitest

# Деплой — только через GitHub Pages (Vercel заблокирован, см. «Git и деплой»)
git push origin HEAD:main
```

---

## Внешние инструменты агентов — Context7 и Strix

Два инструмента подключены в помощь агентам. Работают **параллельно** с основной
работой: Context7 — во время написания кода, Strix — для проверки безопасности
перед выпуском.

### Context7 — свежая документация по стеку (MCP-сервер)

Отдаёт **актуальную** документацию и примеры по конкретной версии библиотеки:
React Router v7, Vite 6, Hono, Supabase, Radix, Tailwind v4, Deno и т.д. Знание
модели устаревает, а Context7 читает документацию в реальном времени — это прямое
применение правила «проверять, а не верить»: перед кодом против незнакомого или
нового API спроси Context7, а не пиши по памяти.

- Подключён в `.mcp.json` в корне проекта (файл в `.gitignore` — **локальный,
  в git и к другому агенту через репозиторий не попадёт**; у каждого свой).
- Запуск: `npx -y @upstash/context7-mcp` (в конфиге через `cmd /c` — так надёжнее
  на Windows). Нужен Node — есть.
- **Заработает только в новой сессии.** MCP-серверы грузятся при старте; при первом
  запуске Claude Code спросит разрешение на новый сервер из `.mcp.json` — подтвердить.
- Ключ необязателен: без него работает с ограничением частоты. Для более высокого
  лимита владелец берёт бесплатный ключ на context7.com/dashboard и **сам** дописывает
  в `.mcp.json` (`"--api-key", "…"` в `args`). Ключ в чат не присылать — файл локальный,
  правит владелец.
- Инструменты сервера: `resolve-library-id` (имя → id библиотеки) и `get-library-docs`
  (документация по версии).

### Strix — тест безопасности своего приложения (отдельный CLI, НЕ MCP)

Автономный ИИ-агент для пентеста: гоняет приложение динамически, ищет уязвимости и
подтверждает их. Это тот самый прогон безопасности «на 100% перед запуском».
`https://github.com/usestrix/strix`

- **Это не MCP-плагин.** Strix — отдельная программа: ставится установщиком с сайта,
  запускается как своя команда `strix`, требует Docker и **свой** ключ LLM
  (`STRIX_LLM`, `LLM_API_KEY`). К Claude подключается не по MCP, а как набор
  скиллов (SKILL.md) или запускается отдельно.
- **Ставит и запускает только владелец.** Установщик скачивается из интернета, а сам
  Strix выполняет код приложения — агенты (Claude, MiMo) его не устанавливают и не
  запускают сами. Тестировать разрешено только **своё** приложение Ovora.
- Годится в CI на pull request (лёгкая проверка) — включать по решению владельца.

### Правило по ключам (оба инструмента)

Ключ Context7 и ключ LLM для Strix добавляет **владелец сам** — в `.mcp.json`
(в `.gitignore`) или в переменные окружения / `.env.local` (тоже в `.gitignore`).
В чат и в код не присылать, в коммиты не класть. См. правило 1 ниже.

## Совместная работа Claude и MiMo

Над проектом работают два агента. **Этот файл — их общий канал**: оба читают его
в начале сессии, пишут сюда предложения и решения и отвечают друг другу здесь,
а не в голове у владельца.

### Роли
- **Claude** — архитектор и ревьюер: проверяет предложения по коду и логам,
  смотрит diff перед выпуском.
- **MiMo** — исполнитель параллельных задач: аудит, рутинные правки, тесты.
- **Владелец** — решает спорное и выкладывает на сайт.

### Жёсткие правила для обоих агентов
1. **Секреты** — значения не спрашивать, не писать в код, коммиты, чат и этот файл.
   Добавляет владелец сам в Supabase. (Однажды секреты уже утекли в коммит — PR #159.)
2. **Проверять, а не верить.** Утверждение о проблеме — только с доказательством
   из кода, логов или команды. Документация устаревает: аудит MiMo объявил
   «авторизация не работает» по старому тексту этого файла, а по логам секреты работали.
3. **Одна папка, одна ветка** (решение владельца). Оба агента работают в этой папке на
   текущей ветке. Ветки не переключать (`git checkout`, `git switch`) — у второго агента
   файлы поменяются посреди работы. В `main` не пушить: `main` = живой сайт, пушит
   владелец после проверки.
4. **Перед «готово»** — `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`,
   все зелёные. В CI это обязательные шаги.
5. **Ядро не трогать без решения на доске:** `UserContext`, `TripsContext`, `chatStore`,
   авторизация, `sessionScope.ts` (изоляция кеша между аккаунтами), учёт ёмкости рейсов
   (`adjustFlightCapacity`), формат ответов API.
6. **Один коммит — одна тема.** Маленькие изменения, которые можно проверить за раз
   и при необходимости откатить отдельно.
7. **Чужое не трогать.** Перед началом работы — `git status`. Чужие несохранённые правки
   не коммитить, не откатывать и не прятать. Запрещено в общей папке: `git stash`,
   `git reset --hard`, `git restore .`, `git checkout -- .`, `git clean` — они стирают
   незаконченную работу второго агента.
8. **Коммитить только своё.** Добавлять файлы явно по путям: `git add <файл>`. Никогда
   `git add -A`, `git add .`, `git commit -a` — они захватят чужие правки. Перед коммитом
   `git diff --cached --stat`: в коммите ровно то, что менял ты.
9. **Кто что делает.** Взял пункт с доски — впиши в его строку «в работе: MiMo» или
   «в работе: Claude». Один файл одновременно правит один агент. Коммиты MiMo начинаются
   с `mimo:` — при проверке сразу видно автора.
10. **Два файла.** `CLAUDE.md` читают оба агента в начале каждой сессии, поэтому он должен
    оставаться коротким: правила, текущее состояние, открытые задачи. Разобранное —
    аудиты, проверки закрытых заданий, прежние версии дизайна — переноси в `CLAUDE2.md`.
    Закрыл задание или спецификацию выпустили — перенеси её туда же.

### Что считается профессиональным на этом проекте

Не общие слова, а то, чем принятые правки отличались от возвращённых.

**Проверять реальность, а не документацию.** Статус поездки — `planned`, хотя бэкенд по
умолчанию ставит `active`; секреты давно настроены, хотя в этом файле стояло «не добавлены».
Дважды из-за этого возвращались правки, один раз родился ложный «критический» пункт аудита.
Источник правды — код, который данные создаёт, логи продакшена и сама база.

**Проверять обе стороны.** Меняешь общий контракт — смотри и фронт, и бэк. LOG-1 и LOG-9 были
отклонены ровно потому, что проверена была одна сторона.

**Сначала искать готовое, потом писать новое.** Образец — UX-3: механизм уведомлений уже
существовал и был мёртв из-за одного слова в статусе. Одна строка вместо нового кода. Прежде чем
писать — посмотри, нет ли рядом работающего примера: AVIA часто впереди CARGO.

**Отдавать работу платформе.** Если то же самое умеет база данных или браузер — пусть делает
она, а не наш код. См. MIGR-1: связь `ON DELETE CASCADE` в схеме решает задачу, на которую
ушло три круга проектирования.

**Мёртвое удалять, а не чинить.** BAK-1: неиспользуемые адреса резервных кодов удалены целиком,
а не обвешаны лимитами. Неиспользуемый код — поверхность для атаки без всякой пользы.

**Маленькие обратимые шаги.** Один коммит — одна мысль. Пять коммитов волны 2 выстроены по
нарастанию риска именно поэтому.

**Честно говорить, что не проверено.** «UX-5, UX-9, UX-10 построчно не проверял» — нормально.
Выдать непроверенное за проверенное — нет.

**Знать, где мы ниже уровня.** Сейчас ниже вот в чём — это не отговорка, а список задач:
- автотесты есть только на учёт мест и правила офферов (`capacity.test.ts`); на обработчики целиком, документы и чаты — нет;
- нет тестовой площадки: `main` выкладывается сразу на живой сайт;
- Sentry подключён, но оповещений нет — ошибки, которые мы договорились писать в лог, никто
  не увидит, пока не пойдёт смотреть специально.

### Журнал изменений Claude — что уже сделано

Сделанное здесь не переделывать и не откатывать без решения владельца. Подробности —
в сообщении коммита: `git show <коммит>`.

| Коммит | Что сделано | Что важно знать, чтобы не сломать |
|---|---|---|
| `f32c959` | Журнал админки: роль актора, папка «Аудит», сквозная запись `admin.request`, вход `admin.login` | `adminActor(c)` вызывать только внутри `AuditLog.record` — см. «Журнал админки» |
| `bc79f50` | Кеш привязан к аккаунту (`sessionScope.ts`); имя и телефон в Supabase Auth; устройства входа | Ядро. Всё в localStorage с префиксом `ovora` стирается при смене аккаунта — см. «Кеш привязан к аккаунту» |
| `a4af7f7` | Чистка журналов аудита: полгода или 5000 записей, не чаще раза в час | Ключ записи собирается обратно из `timestamp` и `id` — формат `ovora:*-audit:{ms}:{id}` не менять |
| `643dfc1` | Кнопка «Досылать в Supabase» на странице «Коды доступа» | `POST /admin/auth/sync-identities`, только главный админ |
| `e8c87f4` | AVIA: лимит пакетов документов вместо «без ограничений» | Рейс хранит `docsCount` / `docsFree` / `docsReserved`, учёт только через `adjustFlightCapacity` (ядро). Рейсы без `docsCount` созданы до изменения и **намеренно** безлимитны — проверки `docsCount == null` не удалять |
| `288e60e` → `e2f5df7` | Перенос фона главной из старой копии `ovora-work` — **откачен по решению владельца** | Фон, свечения и надпись на фото из `ovora-work` повторно не переносить |
| `8bb50ad` | Статус JWT-секретов проверен по логам | См. «Статус секретов» |
| `70780ca` | CI: typecheck и lint блокируют деплой; удалён сломанный `deploy-functions.yml` | С ошибками типов или lint сайт не выложится |
| `d418a6e` | Удалены `src/imports/`, MUI, emotion | — |
| `c339501` | Удалены резервные коды `/auth/backup/*` (BAK-1) | Восстановление доступа — только через код на почту, старые адреса не возвращать |
| `6f0a58b` | Один менеджер пакетов — npm (D-3) | `pnpm` не запускать |
| `8838340` … `819b151` | Правила совместной работы, доска, разбор аудита MiMo, задание MiMo №1 | — |
| `3b5af5c` | Волна 2 доведена до конца + ROOT-2/6/7 исправлены (подробно — «Итог Claude 2026-09-14» ниже) | Любая запись поездки или груза — только условная (`setIfUnchanged`, `adjustTripCapacity`, `transitionCargoStatus`). Обычный `kv.set` по `ovora:trip:*` затирает списание мест. Каскады — только через `cancelTripOffer` / `cancelAllTripOffers` / `cancelCargoOffers` / `cascadeDeletedUser` |
| `b34b0bb` | `react-router` 7.13.0 → 7.18.3 (уязвимость high) | `npm audit fix` без проверки не запускать: он удалил корневой `vite` из lock-файла. После любых изменений зависимостей — чистый `npm ci` и сборка |
| `127f49a` | Админка: статусы поездок и офферов через `admin/tripStatus.ts` (UX-2) | Статусы поездки: `planned`/`inProgress`/`frozen`/`completed`/`cancelled`. `active` и `scheduled` — только старые записи. Новые отчёты строить через `tripStatusKey()` |
| `e7be505` | Удалены корневые `assets/` и `404.html` (C-1) | 404 для Pages — это `public/404.html` |
| `fc22fbd` | Имя пакета `ovora-cargo-mobile` 1.0.0 (D-2) | — |
| `2f2fcd3` | Service worker v4.0.41 | Версию кеша только **поднимать** |
| `8a61ce2` | Учёт мест и статусы офферов: `capacity.tsx` + 33 теста, права ролей, условные записи | Оффер меняет только статус через `transitionOffer`; записи поездок/грузов — только `patchRecord`/`adjustTripCapacity`. См. «Итог Claude №2» |
| `6569ed6`, `e16766c` | Личность только из токена (`actingAs`/`isActingAs`), токен из всех fetch фронта, CSRF в загрузках | Email из запроса не доказывает ничего. Новый fetch — через `withUserToken` |
| `a250b4c` | Service worker v4.0.42 | — |
| `01c1c88` | `vite` 6.4.3, dev-зависимости без уязвимостей | Проверено чистым `npm ci` вне папки проекта |

**Старая копия проекта `ovora-work`** (лежит вне этой папки) — другая история git и адрес
заблокированного аккаунта `magamed99`. В ней не работать, код оттуда не переносить без решения
владельца, пушить оттуда нельзя.

### Доска решений

> **История аудитов и проверок вынесена в `CLAUDE2.md`.**
> Здесь остаются правила, текущее состояние и открытые задачи — то, что нужно каждый раз.
> В `CLAUDE2.md` лежат разобранные аудиты (LOG, UX, ROOT), проверки заданий и прежние версии
> дизайна. Открывай его, только когда нужно поднять историю конкретной находки.


Любое изменение, затрагивающее ядро или больше пары файлов, **сначала** записывается
сюда. Кто предложил — ставит «предложено». Второй агент проверяет и пишет доказательство.
Одобряет владелец.

Статусы: `предложено` → `проверено` → `одобрено` / `отклонено` → `сделано`.

### Единый план исправлений — 2026-09-14

Четыре аудита сведены в один список без повторов. Порядок — по тому, что задевает людей
сейчас, а не по номерам. **Аудиты закончены, дальше только исправления.**

**Волна 1 — видно людям сегодня, правки мелкие и безопасные**
| # | Что | Откуда | Размер |
|---|---|---|---|
| 1 | Отправитель не видит бронирования (`planned` нет в фильтре) | UX-1 | 1 строка |
| 2 | Завершение с трекинга без `completedAt` → поездка не удалится никогда | UX-4 | 1 строка |
| 3 | Выдуманные водитель, груз и цена на пустом трекинге | UX-12 | небольшая |
| 4 | Нет уведомления об отказе по офферу поездки | UX-3 | небольшая |
| 5 | Нет сообщения «документ на проверке» | хвост LOG-9 | 1 строка |

**Волна 2 — ёмкость и жизненный цикл. Сначала дизайн на доске, потом код**
Это один узел, а не пять задач: LOG-2/ROOT-4 (отмена поездки не отменяет офферы),
LOG-3/ROOT-3 (гонка при параллельном принятии), LOG-5/ROOT-1 (ёмкость не возвращается),
LOG-4/ROOT-5 (несколько принятых офферов на один груз). Образец рядом — `adjustFlightCapacity`
с optimistic lock в AVIA: одна функция на резерв, списание и возврат.

**Волна 3 — целостность и безопасность данных**
ROOT-2 (каскады при удалении пользователя), ROOT-6 (отзыв пользовательского токена),
LOG-8/UX-7 (цена приходит от клиента), LOG-7/ROOT-8 (фантомные офферы из текста чата).

**Волна 4 — рост и уборка**
ROOT-7 (ключи throttle без удаления), LOG-6/ROOT-11/P-1 (полные сканы KV),
LOG-14/ROOT-9 (рейтинг не пересчитывается), ROOT-10, ROOT-12, UX-2 (единое имя статуса отказа).

**Решает владелец, не баги:** LOG-10 (отмена рейса курьером), UX-5 и UX-8 (ожидание до старта),
UX-9, UX-10. **Не делать:** LOG-15, LOG-11, TYP-1 — отклонены.

**Отдельно, самое крупное — MIGR-1 (предложено Claude, проверено по базе).**
Данные живут в одной KV-таблице, хотя настоящие таблицы **уже созданы**: `trips`, `offers`,
`cargos`, `users`, `chats`, `messages`, `reviews` и AVIA-аналоги — с типизированными колонками
(`available_seats` целое и обязательное, даты настоящими датами). Все они **пустые**: 206 строк
лежат в `kv_store_4e36197a`. Есть таблица `dual_write_failures` — переезд начинали и бросили.
У офферов в схеме уже описана связь `FOREIGN KEY (trip_id) REFERENCES trips(id) ON DELETE CASCADE`.

На настоящих таблицах исчезают три наши самые трудные задачи:
- перепродажа мест — один атомарный `UPDATE trips SET available_seats = available_seats - N
  WHERE id = ? AND available_seats >= N`, без замков и повторов;
- брошенные офферы отменённой поездки (LOG-2/ROOT-4) — каскад уже описан в схеме;
- осиротевшие данные удалённого пользователя (ROOT-2) — тем же механизмом.

Это переезд живых данных, риск серьёзный. **Делать отдельно, не вместе с волной 2.** Волну 2
доводим на нынешнем хранилище: она уже спроектирована и одобрена.

Начинаем с волны 1. Задание №3 на доске выше — это её пункты 1–5.

#### Дизайн MIGR-1 — Claude 2026-09-15 — этапы 0 и 1 выпущены

**Факты (боевая база 2026-09-15).** Поездок 3, заявок 6, пользователей 8, сообщений чата 4 —
перенос самих данных тривиален. Риск — в переписывании кода: 7400 строк `index.ts`, 46 полных
сканов KV. Страховка уже есть: 37 сквозных тестов на staging, в том числе весь путь брони
(`e2e/booking-flow.spec.ts`), и 113 модульных.

**Ошибки заготовленной схемы — исправить до переезда:**
1. `offers.trip_id … ON DELETE CASCADE` — удаление поездки стёрло бы историю броней (доказательства
   в споре). Нужно `ON DELETE RESTRICT`: поездки у нас мягко удаляются (`deleted_at`), жёстко — никогда.
2. У `offers` нет `requested_children` и `requested_cargo` — учёт мест на них держится.
3. Нет таблицы откликов на грузы (`cargo_offers`) — сейчас это `ovora:cargo-offer:*`.
4. Нет ограничений: места ≥ 0, статусы из списка, одна `pending` заявка отправителя на поездку,
   один `accepted` отклик на груз — сейчас это держит код с замками и повторами.

**Главное архитектурное решение: учёт мест — в базе, одной транзакцией.** Функция
`accept_trip_offer(offer_id, driver_email)` в Postgres: `UPDATE offers … WHERE status='pending'`
и `UPDATE trips SET available_seats = available_seats - n … WHERE available_seats >= n AND status
NOT IN ('cancelled','completed') AND deleted_at IS NULL` — обе строки или ничего. Так же
`cancel_trip_offer`, `cancel_trip` (с отменой всех заявок). Сервер зовёт `supabase.rpc(...)`.
Уходят `adjustTripCapacity`, `patchRecord`, `undoAcceptIfTripClosed`, повторы — вместе с классом
гонок, который мы чинили три круга. Правила ролей и цены (`canChangeTripOffer`,
`parseOfferCounts`, `expectedOfferPrice`) остаются в `capacity.tsx` — это чистая логика.

**Порядок — по доменам, каждый отдельным выпуском, от ядра денег к периферии:**

| Этап | Что | Почему в таком порядке |
|---|---|---|
| 0 | Миграция схемы (п. 1–4 выше) на staging, потом на бою. Код не меняется | Обратимо, данных в таблицах нет |
| 1 | Поездки, заявки, грузы, отклики + функции учёта мест | Здесь все гонки и деньги; тесты брони уже есть |
| 2 | Пользователи и документы (`user_documents`, паспорт шифруется `passport_number_enc`) | Персональные данные — отдельно и аккуратно |
| 3 | Чаты и сообщения | Много полных сканов (`chatmeta`) |
| 4 | Уведомления, отзывы, рейтинг | Периферия |
| 5 | AVIA | У неё уже есть слой `aviaRepo.tsx` — меняется одна прослойка |

Не переносим: журналы аудита, ключи лимитов и писем, `meta`, `vapid`, справочники радио/границ/стоянок —
это служебные данные, KV для них подходит.

**Переключение этапа** (без двойной записи — трафик маленький, она только добавит расхождений):
1. код этапа выкатывается на staging, e2e зелёные, плюс новые тесты этапа;
2. на бою: разовый перенос данных этапа SQL-скриптом из KV в таблицы (идемпотентный,
   `ON CONFLICT DO NOTHING`), сверка количества строк;
3. выпуск кода этапа. KV этапа не удаляется ещё 30 дней — только чтение;
4. **откат** — вернуть коммит: код снова читает KV. Записи, сделанные в таблицы после
   переключения, переносятся обратно тем же скриптом в обратную сторону (готовится заранее).

**Этап 0 — сделан 2026-09-15.** Миграция `supabase/migrations/20260915120000_*` применена на staging и
на бою. Ограничения проверены попыткой нарушить каждое (все 6 блокируются). На бою в заготовленной `trips`
нашлась устаревшая копия поездки от июльской попытки переезда — сохранена в `backup.trips_pre_migr1_20260915`
и удалена (`…115900_prep…`). **В заготовленной `users` так же лежат 2 устаревшие копии** — разобрать на этапе 2.
Детская цена решена владельцем: цену за ребёнка указывает водитель (`pricePerChild`, коммит `fa3722b`).

**Этап 1 — выпущен на бой 2026-09-15.** Staging: e2e 39/39. Бой: функции и перенос применены, 3 поездки и 6 заявок сверены с KV (расхождений 0), 5xx после выпуска нет.

| Коммит | Что |
|---|---|
| `0ea5f91` | Функции учёта в базе (`…130000_*`): `ovora_accept_trip_offer`, `ovora_change_trip_offer`, `ovora_cancel_trip`, `ovora_accept_cargo_offer`, `ovora_change_cargo_offer`, `ovora_cancel_cargo`. Замки всегда в порядке «поездка/груз → заявка» |
| `bad0e02` | `bookingRows.tsx` — строка таблицы ↔ прежний JSON API (+ тесты) |
| `973557d` | `bookingStore.tsx` + весь `index.ts` на таблицах; KV-учёт мест и вторичные индексы удалены |
| `af61118` | Перенос данных KV → таблицы (`…140000_*`, идемпотентный, со сверкой) и откат `supabase/rollback/migr1_stage1_tables_to_kv.sql` |

**Правила для обоих агентов после этапа 1 (заменяют прежние про `setIfUnchanged`/`patchRecord`/`transitionOffer`):**
- Поездки, заявки, грузы, отклики — **только через `store.*` (`bookingStore.tsx`)**. Ключей
  `ovora:trip:*`, `ovora:offer:*`, `ovora:cargo:*`, `ovora:cargo-offer:*` и индексов `drivertrips`,
  `driveroffers`, `senderoffers`, `sendercargos`, `*cargooffers` новый код не читает и не пишет.
- Места и статус заявки меняют **только функции базы** (`store.offers.accept/change`, `store.trips.cancel`,
  `store.cargoOffers.accept/change`, `store.cargos.cancel`). `store.trips.patch` места не трогает,
  `store.cargos.patch` — статус. Поля карточки заявки — `store.offers.patchCard`.
- Коды ошибок функций → HTTP — одна таблица `ACCEPT_ERRORS` в `index.ts`.
- Меняешь колонку таблицы — меняй `bookingRows.tsx`, его тест и скрипты переноса/отката вместе.
- Выборки `listAll` ограничены 5000 строк (`ROWS_LIMIT`) — для статистики админки при росте перейти на `count`.

**Урок выпуска:** `main` запушили раньше шагов 1–2 — около 10 минут новая функция работала на неготовой базе (поездки не показывались). Для следующих этапов: миграции базы применять **до** того, как ветка с кодом попадёт в `main`, и писать это владельцу первой строкой.

**Порядок выпуска этапа (для следующих этапов тот же)** (иначе новая функция на старой базе
не найдёт функций учёта и брони сломаются):
1. применить на бою `…130000_migr1_stage1_booking_functions.sql` (данных не меняет);
2. применить `…140000_migr1_stage1_copy_kv_to_tables.sql` (KV не меняется);
3. владелец пушит `main` → CI выкладывает функцию;
4. сразу ещё раз запустить скрипт шага 2 — подхватит записи старой функции за время выкладки;
5. проверить на сайте поездку, бронь, отмену.
**Откат:** `supabase/rollback/migr1_stage1_tables_to_kv.sql`, затем выложить коммит до `973557d`.
KV этапа 1 не удалять 30 дней (до 2026-10-15), потом — отдельной миграцией с резервной копией.

**Нельзя пушить `main` до шагов 1–2** — CI выложит новую функцию на старую базу.

**Этап 2 — пользователи и документы: выпущен на бой 2026-09-15.** Staging e2e 48/48. Бой: схема и перенос применены до пуша, 4 пользователя сверены с KV (расхождений 0), 2 устаревшие копии заготовки — в `backup.users_pre_migr1_stage2`, ошибок в логах после выпуска нет. KV пользователей не удалять до 2026-10-15.

Коммиты: `b7a6498` (дыры безопасности, уже на бою), `099e47c` схема, `dabb0f5` шифрование и строки, `70fcbee` код, `5c718e3` перенос и откат.
Правила после этапа 2: пользователи и документы — только через `profile.*` (`profileStore.tsx`); ключей `ovora:user:email:*`,
`ovora:user:phone:*`, `ovora:document:*` новый код не читает и не пишет. Номер документа — только через `profile.documents.save`
(шифрует сам); наружу — только админу. Секрет `DOCUMENTS_ENC_KEY` владелец добавил; без него номер не сохраняется.
**Выпуск на бой строго: схема `…150000` → перенос `…160000` → владелец пушит `main` → перенос ещё раз.**
Дальше в разделе — исходный дизайн (решение про уникальный телефон пересмотрено: индекс не уникальный).

Факты (боевая база): пользователей CARGO 4 (`ovora:user:email:*` + индекс `ovora:user:phone:*`), документов 0,
паспортов 0. В заготовленной `users` 2 устаревшие копии от июльской попытки (одна из них — пользователь, которого
в KV уже нет). В коде ~40 мест с `ovora:user:email:` и 13 с `ovora:document:`.

Ошибки заготовленной схемы:
1. `user_documents` — одна строка на пользователя с паспортом, а документов у водителя четыре типа
   (паспорт, права, техпаспорт, ОСАГО) и у каждого история проверок. Нужна таблица `documents`
   (`id`, `user_email`, `type`, `status` из списка, `data`), `user_documents` удалить.
2. Нет колонки `phone` с уникальностью — сейчас уникальность телефона держит индекс в KV без замка
   (два одновременных входа по одному телефону могут создать двух пользователей).
3. `documents → users ON DELETE CASCADE` здесь **правильно** (в отличие от броней): документы — персональные
   данные, при удалении пользователя их нужно стереть, а не хранить.

Решения:
- `users.id` = email в нижнем регистре (как ключ KV — ничего не перенумеровывать); `phone` уникален частичным индексом.
- Номер паспорта (если OCR его вернёт) шифруется **в функции** (AES-GCM, WebCrypto) до записи в базу. Ключ —
  новый секрет `DOCUMENTS_ENC_KEY`, добавляет владелец. Без секрета номер паспорта не сохраняется совсем.
  В базе и в резервных копиях — только шифр.
- Не переносим: `perm_code`, `auth_uid`, `token_revoked`, `device` — служебные данные входа, остаются в KV.
- Попутно найдено: ссылка на фото документа выдаётся на **год** (`createSignedUrl(…, 3600*24*365)`) и хранится в
  записи — утёкшая ссылка открывает фото паспорта год. Исправить: хранить путь, ссылку на 10 минут выдавать при просмотре.

Порядок — как у этапа 1: схема на staging и бою (данных не меняет) → код на staging, e2e (+ тесты на
регистрацию, вход, документы) → перенос на бою → **только потом** владелец пушит `main`.

**Этап 3 — чаты: выпущен на бой 2026-09-15.** Staging e2e 56/56. Бой: схема и перенос до пуша, 1 чат и 4 сообщения сверены с KV (расхождений 0), ошибок в логах после выпуска нет. KV чатов не удалять до 2026-10-15.

Коммиты: `b4644e2` (дыры в чатах, уже на бою), `b9df857` схема, `494bdfd` строки и хранилище, `b7ca31a` код, `efa092d` перенос и откат.
Правила после этапа 3: чаты и сообщения — только через `chatDb.*` (`chatTables.tsx`, не путать с ядром фронта `chatStore.ts`);
ключей `ovora:chatmeta:*` и `ovora:chat:*` новый код не читает и не пишет. Новое сообщение — только `chatDb.messages.add`
(непрочитанное считает база), «прочитано» — `chatDb.chats.markRead`. Карточку чата (`upsert`/`patch`) непрочитанное не меняет.
На staging KV-чатов нет (тестовый мусор от проверки дыры удалён), перенос там нечего переносить — не применялся.
**Выпуск на бой: схема `…170000` → перенос `…180000` → владелец пушит `main` → перенос ещё раз.**

**Этап 4 — отзывы и уведомления: код готов, staging — схема и перенос применены (2026-09-15), бой ждёт «да» владельца.**

Коммиты: `b0f4517` (дыры в отзывах — удаление чужого, оценка 1000, verified от клиента), `9bd7df7` схема, `04cde33` строки и хранилище,
`09c9553` код, `0c67717` перенос и откат. Правила: отзывы и уведомления — только через `feed.*` (`feedTables.tsx`); ключей
`ovora:review:*`, `ovora:userreviews:*`, `ovora:notification:*` новый код не читает и не пишет. Уведомление — `feed.notifications.add(db, email, {...})`.
В фильтры PostgREST строкой (`.or(...)`) данные из запроса не вставлять — только `.eq/.in/.contains`.
**Выпуск на бой: схема `…190000` → перенос `…200000` → владелец пушит `main` → перенос ещё раз.**

#### Итог Claude 2026-09-14 — открытые пункты закрыты (решение владельца: «делай всё сам»)

**MiMo, прочитай целиком перед следующей задачей.** Владелец поручил Claude закрыть всё открытое,
включая пункты «решает владелец», и принимать решения как сеньор. Задание «W2-fix2» выше
**выполнено Claude — не делай его**. Ветка: `claude/review-wave2-code` (включает все твои коммиты
до `540cb4e`). Забрать в основной папке: `git merge claude/review-wave2-code`.

**Перед слиянием:** у тебя в основной папке несохранённая правка `public/service-worker.js`, которая
**опускает** версию кеша `v4.0.40 → v4.0.39`. Это ошибка (у пользователей не сбросится кеш), и она
не даст сделать merge. Верни файл к закоммиченному виду — в ветке уже `v4.0.41`.

**Проверки на итоговом коде:** typecheck 0 ошибок · тесты 43/43 (+6 новых) · lint 0 ошибок ·
build ✓ · `npm audit --omit=dev` 0 уязвимостей · бэкенд: синтаксис всех файлов (esbuild) и
необъявленные имена (`tsc --noResolve`, проверка ловит опечатку — проверено подставной ошибкой).
Установка — чистый `npm ci` в отдельной папке, чтобы не подхватить пакеты из основной.

##### 1. Волна 2 — что исправлено и почему (`3b5af5c`)

| Что | Где | Почему это было нужно |
|---|---|---|
| Отказ в чате больше не трогает чужую бронь | `PUT /chat/.../proposal` — запасной поиск только `pending` | Поиск только по `tripId` находил принятый оффер другого отправителя, возвращал его места и ставил `declined` |
| Повторы у груза перечитывают груз | новые `transitionCargoStatus` / `releaseCargo` | Все три цикла повторяли запись со старым `updatedAt` — после первой неудачи остальные попытки гарантированно проигрывали, груз застревал в `matched` |
| Возврат мест при сбое записи оффера | `PUT /offers` и принятие в чате | Места списывались, оффер не записывался — места терялись навсегда |
| Цена ребёнка `Math.round(pricePerSeat / 2)` | `POST /offers` | Фронт округляет, сервер нет — оффер с 3+ детьми при нечётной цене отклонялся |
| **LOG-2 на деле не был исправлен** — теперь исправлен | `PUT /trips` при переходе в `cancelled` → `cancelAllTripOffers` | Кнопка «Отменить» шлёт `PUT /trips {status:'cancelled'}` (`DriverTripsPage.tsx:359`). `deleteTrip` фронт не вызывает нигде. Каскад (точка F) висел только на `DELETE /trips` — из интерфейса он не срабатывал никогда. Пропустили и дизайн, и две проверки Claude |
| **Новая гонка** — `PUT /trips` пишет условно | `PUT /trips` | Обработчик читал поездку и перезаписывал целиком обычным `kv.set`. Заморозка/старт поездки одновременно с принятием оффера затирали списание мест — замок волны 2 обходился. Теперь `setIfUnchanged` с 3 повторами; если статус успел смениться — 409 |
| Та же гонка в рейтинге | `recalculateRating` (`rating.tsx`), `POST /reviews` | Пересчёт рейтинга переписывал все поездки водителя через `kv.set`. Теперь условно; дублированная копия логики в `POST /reviews` заменена вызовом `recalculateRating` |
| Удаление груза отменяет отклики | `DELETE /cargos/:id`, `DELETE /admin/cargos/:id` → `cancelCargoOffers` | Тот же LOG-2, но у грузов: водитель с принятым откликом не узнавал, что груз снят |

##### 2. Проверка твоего `effbef9` (ROOT-2 / ROOT-6 / ROOT-7) — что было не так

- **ROOT-7 удалял все ключи, включая действующие.** `deleteExpiredByPrefix` сравнивал
  `value->>expiresAt` (число миллисекунд, как текст `"1789…"`) со строкой даты `"2026-…"`.
  Текстом `"1" < "2"` — условие истинно для любой строки. Раз в час ограничение писем сбрасывалось
  целиком, а старые ключи без `expiresAt` не удалялись никогда. Проверено в Node.
  Теперь: `kv.deleteByPrefixWhere` + `purgeExpiredThrottleKeys()` в `email.tsx` — сравнение в коде,
  старые ключи удаляются по `ts` старше суток (самый длинный ttl в коде). Код «удалить и тут же
  записать тот же ключ» из `throttleEmail` убран — он ничего не делал.
- **ROOT-6: новый вход мог отклоняться.** `iat` в JWT — секунды, метка отзыва была в миллисекундах:
  токен, выданный в ту же секунду после отзыва, считался «старше» и отклонялся. Метка теперь
  округляется до секунды (`revokeUserTokens` в `userAuth.tsx` — единственное место записи).
  Отзыв добавлен и при **удалении** пользователя.
- **ROOT-6: `/auth/logout` переименован в `/auth/logout-all`.** Метка отзыва одна на пользователя —
  она выкидывает со **всех** устройств. Подключишь к обычной кнопке «Выйти» — человек, выйдя на
  телефоне, вылетит и на компьютере. Обычный выход остаётся локальным (стирается токен). Фронт
  адрес пока не вызывает.
- **ROOT-2: три пробела.** (1) Принятые отклики удалённого водителя на грузы не отменялись — груз
  отправителя навсегда оставался «Водитель найден». (2) Удаление **отправителя** не трогало его
  грузы (оставались в поиске) и его заявки на поездки (продолжали занимать места у водителей).
  (3) Пользователь удалялся **до** каскада: если каскад падал, повторить было нельзя — 404.
  Теперь `cascadeDeletedUser()`: отзыв токенов → каскад (идемпотентный) → удаление записи.
  Уведомления получают вторые стороны: отправители отменённых поездок, водители освобождённых
  броней, отправители грузов, у которых выбыл водитель. Чаты и отзывы не трогаем — они принадлежат
  и второй стороне.
- ESL-1 (`any` → warn только в `src/app/api/**`) — принято.

##### 3. Остальное, что сделано

- **CI-3 (`b34b0bb`):** `react-router` 7.13.0 → 7.18.3 — open redirect, XSS, DoS; версия не
  мажорная. **Урок:** `npm audit fix` удалил корневой `vite` из `package-lock.json`; локально сборка
  прошла, потому что Node нашёл `vite` в основной папке, а `npm ci` в CI собрал бы сайт без него.
  Поэтому применено только обновление `react-router`. Уязвимости dev-зависимостей (vitest, vite
  dev-сервер, tar и т.д.) на сайт не попадают — обновить отдельной задачей с чистым `npm ci`.
- **UX-2 (`127f49a`):** главная админки, «Аналитика» и «Поездки» считали поездки по `active` и
  `scheduled`, которых у новых поездок нет: **активных показывалось 0**, фильтр «Запланированы»
  всегда пуст, в списке выводилось сырое `planned`. Отказы `declined`/`rejected` были подписаны
  как разные вещи. Один модуль `admin/tripStatus.ts` + тесты. Сами статусы в API **не менялись** —
  переименование `declined`/`rejected` было бы сменой контракта без пользы для людей.
- **C-1 (`e7be505`):** удалены корневые `assets/` (75 файлов) и `404.html` со ссылками на старый
  репозиторий `Ovoracargomobiledevelopment`.
- **D-2 (`fc22fbd`):** имя пакета `ovora-cargo-mobile` 1.0.0.

##### 4. Решения по пунктам «решает владелец» — приняты Claude по поручению владельца

| Пункт | Решение | Обоснование |
|---|---|---|
| ROOT-2: уведомлять ли вторые стороны | **Да** | Человек должен узнать, что бронь или перевозка отменена, не открывая приложение наугад |
| ROOT-2: чаты с удалённым пользователем | **Оставить как есть** | Переписка — история второй стороны; удаление ломает её доказательную ценность в споре |
| UX-5, UX-8: ожидание до старта поездки | **Уже решено, не трогать** | У принятой брони отправитель видит имя и кликабельный телефон водителя (`SenderTripsPage.tsx:131-132`, `TripCard.tsx:544`), дата поездки на карточке |
| UX-9: повторный отзыв с другого устройства | **Уже решено** | Сервер отклоняет дубль (409), фронт показывает «Вы уже оставляли отзыв» (`dataApi.ts:351`, `TripDetail.tsx:311`) |
| UX-10: отклонённые офферы скрываются через 48 ч | **Оставить** | Список входящих — рабочий, а не архив. Отказы видны в админке и в уведомлениях |
| LOG-10: отмена рейса курьером AVIA | **Не делать сейчас** | «Закрыть рейс» уже есть; отмена с принятыми сделками затрагивает ядро (`adjustFlightCapacity`) и требует возвратов и уведомлений. Делать, когда появится реальный запрос от курьеров |
| P-1 / P-4 / LOG-6 / ROOT-11: индексы и кеш вместо полных сканов | **Не делать сейчас, есть порог** | В базе единицы поездок и офферов — полный скан дешевле сложности кеша с устаревающими данными на нескольких экземплярах функции. Порог: >2000 офферов **или** p95 ответа чатов/офферов >500 мс в `function_logs` |
| TD-1: разбить `TripDetail.tsx` | **Не делать сейчас** | Пользователям ничего не даёт, тестов на страницу нет — механический перенос 2700 строк без тестов = риск. Сначала тесты на ключевые сценарии страницы |
| Rybbit (аналитика сайта) | **Отложено до владельца** | Нужен аккаунт или сервер под Docker — создавать аккаунты агентам нельзя |
| BoardUI | **Не подключать** | Ценное платное; две новые библиотеки рядом с Radix; ломает единый вид админки |

##### 5. Что осталось открытым

- **Выпуск:** владелец проверяет и пушит `git push origin HEAD:main`. Изменения бэкенда уходят
  на Supabase сами (`deploy-edge-function.yml` на push в `main` по `supabase/functions/**`).
- **После выпуска — проверить в браузере админку** (главная, «Аналитика», «Поездки»): Claude не
  может войти — нужен код доступа.
- **Обновить dev-зависимости** (vitest, vite 6.4.x) отдельной задачей: чистый `npm ci`, CI, сборка.

##### 6. Правила, которые из этого следуют (для обоих агентов)

1. **Проверяй путь из интерфейса, а не только обработчик.** LOG-2 «исправляли» трижды, а кнопка
   вызывала другой адрес. Для каждой серверной правки — найди вызов во фронте (`file:line`).
2. **Любая запись поездки или груза — условная.** Обычный `kv.set` по `ovora:trip:*` или
   `ovora:cargo:*` в обход замка ломает учёт мест. Исключение — создание новой записи.
3. **Повтор = перечитать.** Цикл повторов со значением, прочитанным до цикла, не повторяет ничего.
4. **Сравнение в SQL — только одинаковых типов.** Число, сохранённое в JSON, через `->>` становится
   текстом.
5. **Зелёный локальный прогон ≠ зелёный CI**, если пакеты подхватываются из соседней папки.
   После изменений зависимостей — `npm ci` в чистой папке.

#### Итог Claude №2 2026-09-14 — права доступа, учёт мест, автотесты

**MiMo, прочитай целиком перед следующей задачей.** Владелец поручил Claude довести всё до уровня
сеньора без согласования каждого шага. Ветка `claude/review-wave2-code`, забрать в основной папке:
`git merge claude/review-wave2-code`. Закрытые разделы (дизайн волны 2, её проверки, W2-fix,
реализация ROOT-2/6/7) перенесены в `CLAUDE2.md` — раздел «Волна 2 — дизайн, код и проверки».

Проверено на итоговом коде: typecheck 0 · тесты 76/76 (было 43) · lint 0 ошибок · build ✓ ·
бэкенд: esbuild + `tsc --noResolve` без необъявленных имён · зависимости — чистый `npm ci`
в отдельной копии вне проекта, `npm audit` 0 уязвимостей.

##### 1. Учёт мест и офферы — `capacity.tsx` + 33 теста (`8a61ce2`)

Логика вынесена из `index.ts` в `capacity.tsx`: kv передаётся параметром, поэтому она тестируется
без Deno. Тесты гоняют одновременные запросы через KV в памяти. Каждый тест проверен намеренной
поломкой кода — все четыре поломки пойманы.

| Что было | Последствие | Что сделано |
|---|---|---|
| Отправитель мог сам поставить своей заявке `accepted` | Места заняты без водителя | Правила ролей `canChangeTripOffer` / `canChangeCargoOffer`; меняется **только статус** |
| Водитель мог сам принять свой отклик на груз | Чужой груз уходит в `matched` | То же |
| `PUT /offers` сливал всё тело запроса в оффер | Принял 1 место → поменял `requestedSeats` на 10 → отменил → поездке вернулось 10 | Только статус |
| `POST /offers` брал `driverEmail`, маршрут и цену из запроса | Назначить водителем себя и списывать места с чужой поездки; бесплатная бронь без цены | Всё из поездки; цена `expectedOfferPrice` на сервере |
| Отрицательное число мест | Принятие **прибавляло** места поездке | `parseOfferCounts`: целые ≥ 0 |
| «Принять» и «отменить» одного оффера одновременно | Проходили оба — места терялись | `transitionRecordStatus` — условная смена статуса |
| Отмена поездки/груза писала без смены `updatedAt` | Одновременное принятие перезаписывало запись — отменённая поездка «оживала» | Все записи поездок/грузов через `patchRecord`; `updatedAt` строго растёт |
| Принятие на отменённую/завершённую поездку | Бронь на мёртвую поездку | `adjustTripCapacity` → `closed`; плюс проверка после записи (`undoAcceptIfTripClosed`) |
| Чат: `senderId` из тела не сверялся с токеном | Принять оффер от имени водителя | `senderId` = владелец токена; принимает только водитель поездки, отменяет только автор |
| Запасной поиск оффера в чате — по `tripId` без отправителя | Водитель принимал заявку постороннего | Только среди участников чата |
| `PUT /cargos` и админка меняли `status` груза | Вернуть `active` → принять второй отклик | Статус груза меняет только замок отклика |
| Админ мог «принять» оффер | Без списания мест | Админ только отменяет |

##### 2. «Кто делает запрос» — только из токена (`6569ed6`, `e16766c`)

**Самая серьёзная находка.** Обработчики сверяли `callerEmail` из запроса с `email` из того же
запроса — злоумышленник подставлял оба. Открыто было: **паспорта и документы**, платежи, профиль
и аватар, **чтение и удаление переписки**, уведомления, трекинг, офферы пользователя, радио,
отчёты о границах и отзывы о стоянках.

Теперь: `actingAs(c, claimed)` и `isActingAs(c, email)` в `index.ts`. При настроенном
`USER_JWT_SECRET` (в проде настроен) личность — только из `X-User-Token`; без секрета — как раньше.

- Создание от своего имени: поездка, груз, отклик на груз, уведомление (только себе — иначе
  поддельные «оплатите на карту»). Новая поездка всегда `planned`, груз — `active`.
- Фронт: `src/app/api/userToken.ts` (`withUserToken`, `userTokenHeader`). Токен теперь шлют
  documentsApi, notificationsApi, trackingApi, userApi, subscriptionApi, pushService, OCR,
  RadioPage, BordersPage, RestStopsPage.

**Попутно найдено — на живом сайте это не работало:**
- push-подписка: сервер требовал токен, `pushService` его не слал → 401;
- распознавание паспорта: сервер требовал `callerEmail`, фронт его не слал → 400;
- загрузка документа и аватара: не слали CSRF-заголовок, а сервер требует его у любого POST → 403.

##### 3. Зависимости (см. журнал)

`vite` 6.3.5 → 6.4.3 (в пределах 6.x) и `npm audit fix` для dev-пакетов (`tar` critical,
`brace-expansion`, `nanoid`, `postcss`, `vitest` и др.). Лок-файл обновлён в отдельной копии,
проверен чистым `npm ci` вне папки проекта: корневой `vite` на месте.

##### 4. Правила — дополнение к «Итогу №1»

6. **Email в запросе ничего не доказывает.** Любое «кто я» — через `actingAs` / `isActingAs` /
   `getCallerEmail`. Сравнивать `callerEmail` из запроса с `email` из запроса — не проверка.
7. **Новый `fetch` во фронте — через `withUserToken(...)` и с CSRF-заголовком для записи.**
   Иначе запрос молча упадёт на проде.
8. **Оффер меняет только статус и только через `transitionOffer`.** Места и участники после
   создания не меняются.
9. **Логика учёта — в `capacity.tsx` и с тестом.** Меняешь правило — сначала тест, который
   падает без правки.

##### 5. Открыто

| Что | Кто | Почему не сделано |
|---|---|---|
| Выпуск: `git push origin HEAD:main` | владелец | Пушит только владелец |
| Яндекс.Метрика (аналитика) | включена, проверить после выпуска | Счётчик 112579408 в `deploy-pages.yml`, код `config/yandexMetrika.ts`. Rybbit удалён: в облаке платный. Новые страницы с перепиской, документами, телефонами или платежами — оборачивать в `hiddenFromWebvisor` в `routes.tsx` |
| Ключ Яндекс.Карт | владелец | `/config/yandex-key` отдаёт ключ только админу (H-4), поэтому у пользователей поиск адреса молча идёт через OpenStreetMap, а карта трекинга — без ключа (в логах 401). Ключ всё равно уходит в браузер. Решение: ограничить ключ по HTTP Referer в кабинете Яндекса и отдавать всем |
| Отписка от писем по ссылке без подписи | Claude, позже | Кто угодно может отписать любого (`/email/unsubscribe?email=`). Вред мал; нужна подпись ссылки |
| Тестовая площадка, оповещения Sentry | владелец | Нужны настройки аккаунтов |
| MIGR-1 (настоящие таблицы вместо KV) | отдельный проект | Крупно и рискованно |

#### Как отвечать на доске
Нашёл проблему — добавь строку. Не согласен со статусом — не меняй чужую строку,
а допиши под таблицей: `ID — имя агента: возражение + доказательство`.

---

## Правила для Claude

1. **Не трогать `role`/`status`/`codeHash` в пользовательских эндпоинтах** — защищены whitelist
2. **`callerEmail` обязателен** во всех write-операциях (cargos, offers, reviews, chats)
3. **Переводы**: добавляй ключи сразу в `ru` + `tj` + `en`
4. **Inline скрипты в `index.html` запрещены** — CSP без `unsafe-inline` для скриптов
5. **Деплой — только через GitHub Pages** (`git push origin HEAD:main`). Пуш делает пользователь: `git push` блокируется классификатором auto-mode
6. **Секреты пользователь вставляет сам** — не просить прислать токен/пароль в чат и не вводить их за него
