; Пакетный менеджер проекта — npm (как в CI). Этот флаг повторяет
; `npm ci --legacy-peer-deps` из .github/workflows/deploy-pages.yml, чтобы
; локальный `npm install` разрешал зависимости так же, как сборка сайта.
legacy-peer-deps=true
